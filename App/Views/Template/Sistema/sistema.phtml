<?php

if (!isset($GLOBALS['hashIdentificaIndexAtan']) || $GLOBALS['hashIdentificaIndexAtan'] != md5($GLOBALS['chave_md5']))
{
    die("Acesso negado");
}

?>
<pre>


    
    <h3>
        Utilizando template sistema admin login
        Layout do Sistema ou site. Para entrar no admin tem que digitar na url /admin para acessar o controller admin...
    </h3>
</pre>